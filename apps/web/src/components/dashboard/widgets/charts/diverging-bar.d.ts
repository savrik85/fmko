export interface DivergingDatum {
    label: string;
    positive: number;
    negative: number;
}
export declare function DivergingBar({ data, positiveLabel, negativeLabel, }: {
    data: DivergingDatum[];
    positiveLabel: string;
    negativeLabel: string;
}): import("react").JSX.Element;
//# sourceMappingURL=diverging-bar.d.ts.map