# VIP lóže: plán implementace

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Nové zařízení stadionu `vip_box` (VIP lóže, úrovně 0–3), které bere místa platícím divákům a stojí provoz za každý domácí zápas, zato zvedá šanci, že majitel firmy pozvání přijme, jeho náklonnost po zápase a přízeň zastupitelů obce, kteří zápas prosedí. Ve 3D je to prosklená lóže na hlavní tribuně.

**Architecture:** Všechna čísla lóže žijí na jednom místě, v `SKALY.vip_box` v `apps/api/src/stadium/stadium-generator.ts`, a do hry se dostávají přes `calculateFacilityEffects` (kapacita a náklad) a přes čisté funkce v `sponsors/favor-math.ts` (majitelé). Kapacita se snižuje uvnitř `capacityBonus`, takže ji automaticky převezmou match-runner, pohár, GET stadionu i rubrika kotle. Náklad jde jako `match_expense` ve `processMatchDayFinances`. Majitelé dostanou bonus v `settleSponsorInvitations` a v invite routě, zastupitelé v novém helperu `villages/vip-box-favor.ts`, volaném v match-runneru až za zámkem zápasu. 3D kreslí nová komponenta `stadium-3d/VipBox.tsx`.

**Tech Stack:** Hono + Cloudflare D1 (SQLite), vitest, Next.js 15 (client komponenty), react-three-fiber, Tailwind.

**Spec:** Schválený návrh v konverzaci 2026-09-22, shrnut v sekci Design níže.

## Design

- Klíč `vip_box`, úrovně 0–3, sloupec `stadiums.vip_box INTEGER NOT NULL DEFAULT 0` (migrace 0218, ručně na test; produkce jen po výslovném souhlasu).
- Kapacita pro platící diváky: L1 −30, L2 −60, L3 −100 míst. Menší návštěva znamená menší vstupné i tržby z občerstvení, samo z existující matematiky (kapacita je strop návštěvy).
- Provoz za domácí soutěžní zápas (raut, obsluha): 1 500 / 3 000 / 5 000 Kč, zaúčtováno ve financích zápasu.
- Cena stavby zhruba jako tribuny: 55 000 / 170 000 / 450 000 Kč, stejný nákupní tok jako ostatní zařízení (`UPGRADE_COSTS` + `POST /api/teams/:teamId/stadium/upgrade`; upgrade je v kódu okamžitý, sloupce `upgrade_facility`/`upgrade_completes_at` neexistují).
- Majitelé firem pozvaní na domácí zápas: po zápase navíc +1/+2/+3 náklonnosti, šance na přijetí pozvání +5/+10/+15 procentních bodů.
- Zastupitelé obce: každý, kdo zápas prosedí (`village_invitations.status = 'attended'`), dostane +1/+2/+3 přízně (osobní řádek ve `village_team_favor`), jednou za zápas.
- 3D: prosklená lóže na hlavní tribuně, L1 jedna kabina, L2 tři, L3 celá řada pod markýzou.
- Etapa 2 (lóže jako slib sponzorovi) je mimo rozsah; úroveň se čte prostě jako `facilities.vip_box` / `stadiums.vip_box`.
- Rozhodnutí z plánu (drobné, v duchu návrhu): lóže vyžaduje aspoň tribuny L1 (stejný zámek jako zastřešení), jinak ji ve 3D není kam postavit. Dosavadní dekorativní „VIP skybox" u tribun L3 v `Stand.tsx` se ruší, aby lóži na stadionu neměl i klub, který si ji nekoupil.

## Global Constraints

- Identifikátory v kódu anglicky, texty pro hráče a komentáře česky.
- V textech pro hráče nikdy dlouhá pomlčka (—). Znaménko minus (−, U+2212) u čísel je v pořádku, používá ho i stávající kód.
- Nikdy prázdný `catch`. Server `logger.warn/error({ module }, "popis", e)`; `logger` serializuje z kontextu jen `teamId`, `matchId`, `playerId`, `reqId`, ostatní id patří do textu zprávy. Klient `console.error("popis:", e)`.
- UI: minimum `text-sm`, ceny nikdy v tlačítkách, mobil nejdřív, žádné nové sloupce v tabulkách, vše česky.
- Testovací DB `prales-db-test`; produkce jen po výslovném souhlasu. D1 dotazy: vnější `'`, vnitřní `"`, žádný backslash před `$`.
- Pracuje se na větvi `testing`. V implementačních úlohách (1–6) se NEPUSHUJE; push jen v úloze 7. Nic na `main`.
- Commit zprávy končí prázdným řádkem a `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>`.
- `apps/api/src/stadium/facility-keys.test.ts` musí po každé úloze projít: klíč musí být ve finance-processor, match-runner, routes/game.ts, resolve-match-incidents a ve čtyřech FE mapách. Proto je registrace klíče celá v úloze 1.

## Souborová struktura

| Soubor | Odpovědnost |
|---|---|
| `apps/api/migrations/0218_vip_box.sql` | nový sloupec `stadiums.vip_box` |
| `apps/api/src/stadium/stadium-generator.ts` | `SKALY.vip_box`, popisek, cena, texty úrovní, popis přírůstku, zámek na tribuny, nová pole v `StadiumFacilityEffects`, kapacita |
| `apps/api/src/stadium/vip-box.test.ts` (nový) | unit testy katalogu, kapacity, nákladu a textu nabídky |
| `apps/api/src/stadium/vip-box-wiring.test.ts` (nový) | hlídač, že každé místo počítající kapacitu z `{ stands }` posílá i `vip_box` a že finance lóži strhávají |
| `apps/api/src/season/finance-processor.ts` | klíč v seznamu zařízení, strhnutí provozu lóže |
| `apps/api/src/multiplayer/match-runner.ts` | klíč v seznamu zařízení, úroveň lóže do `settleSponsorInvitations`, volání přízně zastupitelů |
| `apps/api/src/routes/game.ts` | klíč ve dvou objektech `facilities`, shrnutí `vipBox` v GET stadionu |
| `apps/api/src/fans/resolve-match-incidents.ts` | klíč v SELECTu stadionu |
| `apps/api/src/news/ultras-report.ts` | klíč ve `FACILITY_KEYS` a `s.vip_box` v SELECTu (správná kapacita) |
| `apps/api/src/routes/matches.ts`, `apps/api/src/routes/teams.ts` | kapacita v náhledu zápasu, profilu týmu a klubu počítá i s lóží |
| `apps/api/src/sponsors/favor-math.ts` (+ `.test.ts`) | `vipBoxFavorBonus`, `vipBoxAcceptanceBonus`, parametr `vipBoxLevel` v `invitationAcceptance` |
| `apps/api/src/sponsors/hooks.ts` (+ nový `hooks.test.ts`) | parametr `vipBoxLevel` v `settleSponsorInvitations` |
| `apps/api/src/routes/sponsors.ts` | invite route čte úroveň lóže |
| `apps/api/src/villages/vip-box-favor.ts` (+ `.test.ts`) (nový) | přízeň zastupitelů, kteří zápas prosedí v lóži |
| `apps/web/src/app/(hra)/stadion/page.tsx` | ikona, popisek, popisy úrovní, řádek „Teď:" u karty lóže |
| `apps/web/src/components/dashboard/widgets/items/fans-widgets.tsx` | popisek v radaru |
| `apps/web/src/components/stadium/stadium-view.tsx` | čip lóže ve 2D náhledu |
| `apps/web/src/components/stadium/stadium-3d/VipBox.tsx` (nový) | 3D lóže |
| `apps/web/src/components/stadium/stadium-3d/Stadium3D.tsx` | vykreslení `VipBox` |
| `apps/web/src/components/stadium/stadium-3d/Stand.tsx` | odstranění dekorativního `VIPBox` u tribun L3 |

---

### Task 1: Migrace 0218, katalog a kapacita, registrace klíče všude

**Files:**
- Create: `apps/api/migrations/0218_vip_box.sql`
- Create: `apps/api/src/stadium/vip-box.test.ts`
- Modify: `apps/api/src/stadium/stadium-generator.ts` (`FACILITY_LABELS` ř. ~97, `UPGRADE_COSTS` ř. ~116, `SKALY` ř. ~140, `UPGRADE_EFFECTS` ř. ~211, `popisPrirustku` ř. ~248, `getUpgradeOptions` ř. ~386, `StadiumFacilityEffects` ř. ~423, `calculateFacilityEffects` ř. ~446)
- Modify: `apps/api/src/season/finance-processor.ts:422`
- Modify: `apps/api/src/multiplayer/match-runner.ts:343`
- Modify: `apps/api/src/routes/game.ts:1564-1578` a `:1901-1915`
- Modify: `apps/api/src/fans/resolve-match-incidents.ts:440-441`
- Modify: `apps/api/src/news/ultras-report.ts:41` a `:165`
- Modify: `apps/web/src/app/(hra)/stadion/page.tsx:142-191`
- Modify: `apps/web/src/components/dashboard/widgets/items/fans-widgets.tsx:124-138`

**Interfaces:**
- Consumes: nic nového.
- Produces:
  - DB sloupec `stadiums.vip_box INTEGER NOT NULL DEFAULT 0`.
  - `SKALY.vip_box: { seatsLost: readonly [0, 30, 60, 100]; matchCost: readonly [0, 1500, 3000, 5000]; sponsorFavor: readonly [0, 1, 2, 3]; sponsorAcceptance: readonly [0, 0.05, 0.1, 0.15]; villageFavor: readonly [0, 1, 2, 3] }`
  - Nová pole `StadiumFacilityEffects`: `vipBoxCapacityLoss: number`, `vipBoxMatchCost: number`, `vipBoxSponsorFavorBonus: number`, `vipBoxSponsorAcceptanceBonus: number` (podíl 0–1), `vipBoxVillageFavorBonus: number`.
  - `capacityBonus` = `STANDS_CAPACITY[stands] − SKALY.vip_box.seatsLost[vip_box]` (může být záporné).
  - `FACILITY_LABELS.vip_box = "VIP lóže"`, `UPGRADE_COSTS.vip_box = [0, 55000, 170000, 450000]`.

- [ ] **Step 1: Napsat migraci**

`apps/api/migrations/0218_vip_box.sql`:

```sql
-- 0218: VIP lóže na hlavní tribuně, úroveň 0–3.
-- Aplikovat ručně: npx wrangler d1 execute prales-db-test --remote --file apps/api/migrations/0218_vip_box.sql
-- Produkce jen po výslovném souhlasu a po záloze (wrangler d1 export).
ALTER TABLE stadiums ADD COLUMN vip_box INTEGER NOT NULL DEFAULT 0;
```

- [ ] **Step 2: Napsat padající test**

`apps/api/src/stadium/vip-box.test.ts`:

```ts
/**
 * VIP lóže: kapacita, náklad a text v nabídce upgradu vycházejí z jediné škály.
 */
import { describe, expect, it } from "vitest";
import {
  calculateFacilityEffects, FACILITY_LABELS, getUpgradeOptions, SKALY, UPGRADE_COSTS,
} from "./stadium-generator";

describe("VIP lóže v katalogu", () => {
  it("má popisek a ceny jako tribuny", () => {
    expect(FACILITY_LABELS.vip_box).toBe("VIP lóže");
    expect(UPGRADE_COSTS.vip_box).toEqual([0, 55000, 170000, 450000]);
  });

  it("bere platícím divákům 30/60/100 míst", () => {
    const kap = (vip: number) => calculateFacilityEffects({ stands: 3, vip_box: vip }).capacityBonus;
    expect([0, 1, 2, 3].map(kap)).toEqual([500, 470, 440, 400]);
    expect(calculateFacilityEffects({ stands: 1, vip_box: 1 }).capacityBonus).toBe(60);
    expect(calculateFacilityEffects({ vip_box: 3 }).vipBoxCapacityLoss).toBe(100);
  });

  it("stojí 1 500 / 3 000 / 5 000 Kč za domácí zápas", () => {
    expect([0, 1, 2, 3].map((l) => calculateFacilityEffects({ vip_box: l }).vipBoxMatchCost))
      .toEqual([0, 1500, 3000, 5000]);
  });

  it("bonusy pro majitele a zastupitele rostou s úrovní", () => {
    const fx = (l: number) => calculateFacilityEffects({ vip_box: l });
    expect([0, 1, 2, 3].map((l) => fx(l).vipBoxSponsorFavorBonus)).toEqual([0, 1, 2, 3]);
    expect([0, 1, 2, 3].map((l) => fx(l).vipBoxSponsorAcceptanceBonus)).toEqual([0, 0.05, 0.1, 0.15]);
    expect([0, 1, 2, 3].map((l) => fx(l).vipBoxVillageFavorBonus)).toEqual([0, 1, 2, 3]);
  });

  it("nesmyslná úroveň se ořízne na 0–3", () => {
    expect(calculateFacilityEffects({ vip_box: 9 }).vipBoxMatchCost).toBe(5000);
    expect(calculateFacilityEffects({ vip_box: -2 }).vipBoxMatchCost).toBe(0);
  });

  it("bez tribuny je zamčená, s tribunou ne", () => {
    const bez = getUpgradeOptions({ stands: 0, vip_box: 0 }, 100, 100, 10, true).find((o) => o.facility === "vip_box");
    expect(bez?.locked).toBe(true);
    expect(bez?.lockDetail?.prerequisite).toBe("Nejdřív postav aspoň základní tribuny");
    const s = getUpgradeOptions({ stands: 1, vip_box: 0 }, 100, 100, 10, true).find((o) => o.facility === "vip_box");
    expect(s?.locked).toBe(false);
  });

  it("nabídka slibuje přesně přírůstek, ztráta míst jde první", () => {
    for (const current of [0, 1, 2]) {
      const o = getUpgradeOptions({ stands: 3, vip_box: current }, 100, 100, 10, true).find((x) => x.facility === "vip_box")!;
      const ztrata = SKALY.vip_box.seatsLost[current + 1] - SKALY.vip_box.seatsLost[current];
      const prvni = o.effect.match(/([+−-])\s*([\d.,]+)/);
      expect(prvni?.[1]).toBe("−");
      expect(Number(prvni?.[2])).toBe(ztrata);
      expect(o.effect).toContain(`provoz ${SKALY.vip_box.matchCost[current + 1]} Kč za domácí zápas`);
      expect(o.effect).not.toContain("—");
    }
  });
});
```

- [ ] **Step 3: Ověřit, že test padá**

Run: `cd apps/api && npx vitest run src/stadium/vip-box.test.ts`
Expected: FAIL (`FACILITY_LABELS.vip_box` je `undefined`, `SKALY.vip_box` neexistuje).

- [ ] **Step 4: Katalog v `stadium-generator.ts`**

V `FACILITY_LABELS` za řádek `  cage: "Klec nad kotlem",` přidat:

```ts
  vip_box: "VIP lóže",
```

V `UPGRADE_COSTS` za řádek `  cage: [0, 26000, 70000, 0],` přidat:

```ts
  // Zhruba jako tribuny: lóže je stavba na tribuně, ne kus vybavení.
  vip_box: [0, 55000, 170000, 450000],
```

V `SKALY` za blok `cage: { ... },` (končí řádkem `    tlumeniHlasu: [0, 0.1, 0.2, 0.2],` a `  },`) a před `} as const;` přidat:

```ts
  /**
   * VIP lóže na hlavní tribuně. Zabírá místa platícím divákům a každý domácí
   * soutěžní zápas stojí raut a obsluhu. Vrací se přes lidi, které klub na
   * zápas pozve: majitele firem (náklonnost, šance, že pozvání přijmou) a
   * zastupitele obce (přízeň). `sponsorAcceptance` je podíl, 0,05 = 5 p. b.
   * Čte to `calculateFacilityEffects` i `sponsors/favor-math.ts`.
   */
  vip_box: {
    seatsLost: [0, 30, 60, 100],
    matchCost: [0, 1500, 3000, 5000],
    sponsorFavor: [0, 1, 2, 3],
    sponsorAcceptance: [0, 0.05, 0.1, 0.15],
    villageFavor: [0, 1, 2, 3],
  },
```

V `UPGRADE_EFFECTS` za řádek `  cage: ["", "Mříž mezi kotlem a hřištěm", "Plexi až do výšky, na trávník se nedostane nic", ""],` přidat:

```ts
  vip_box: ["", "Prosklená kabina pro pár hostů", "Tři kabiny s rautem a obsluhou", "Celá řada lóží pod markýzou"],
```

V `popisPrirustku` před `    default:` přidat:

```ts
    case "vip_box": {
      // Ztráta míst jde PRVNÍ: hráč má vidět, co lóže bere, dřív než co dává.
      const v = SKALY.vip_box;
      const casti = [`−${rozdil(v.seatsLost)} míst pro platící diváky`];
      casti.push(`+${rozdil(v.sponsorFavor)} náklonnost pozvaného majitele po zápase`);
      casti.push(`+${pct(rozdil(v.sponsorAcceptance))} šance, že majitel pozvání přijme`);
      casti.push(`+${rozdil(v.villageFavor)} přízeň každého zastupitele na tribuně`);
      // Bez znaménka, ať to regex testů nesebere místo ztráty míst.
      casti.push(`provoz ${v.matchCost[to]} Kč za domácí zápas`);
      return casti.join(", ");
    }
```

V `getUpgradeOptions` za blok (anchor):

```ts
    if (key === "security" && (stadium.fence ?? 0) < 1) {
      locked = true;
      lockDetail.prerequisite = "Nejdřív postav aspoň provizorní oplocení";
    }
```

přidat:

```ts
    // Lóže sedí na hlavní tribuně. Bez tribuny ji není kam postavit.
    if (key === "vip_box" && (stadium.stands ?? 0) < 1) {
      locked = true;
      lockDetail.prerequisite = "Nejdřív postav aspoň základní tribuny";
    }
```

V `interface StadiumFacilityEffects` za řádek `  sectorSeparation: number;       // oplocení: o kolik klesne riziko střetu s hostujícím kotlem` přidat:

```ts
  vipBoxCapacityLoss: number;           // VIP lóže: kolik míst pro platící diváky zabere
  vipBoxMatchCost: number;              // VIP lóže: Kč za domácí soutěžní zápas (raut, obsluha)
  vipBoxSponsorFavorBonus: number;      // VIP lóže: + náklonnost pozvaného majitele po zápase
  vipBoxSponsorAcceptanceBonus: number; // VIP lóže: + šance (0–1), že majitel pozvání přijme
  vipBoxVillageFavorBonus: number;      // VIP lóže: + přízeň každého zastupitele, který zápas prosedí
```

V `calculateFacilityEffects` za řádek `  const ca = facilities.cage ?? 0;` přidat:

```ts
  const vb = Math.max(0, Math.min(3, Math.round(facilities.vip_box ?? 0)));
```

a nahradit:

```ts
    // Jediné, co dělá rozdíl v kapacitě — základ má každý klub stejný.
    capacityBonus: STANDS_CAPACITY[st] ?? 0,
```

za:

```ts
    // Kapacitu mění tribuny (přidávají) a VIP lóže (bere místa platícím).
    // Základ má každý klub stejný. Lóže odečtená tady platí všude, kde se
    // kapacita čte: zápas, pohár, stránka stadionu i rubrika kotle.
    capacityBonus: (STANDS_CAPACITY[st] ?? 0) - SKALY.vip_box.seatsLost[vb],
```

a za řádek `    sectorSeparation: SKALY.fence.oddeleni[fe] ?? 0,` přidat:

```ts
    vipBoxCapacityLoss: SKALY.vip_box.seatsLost[vb],
    vipBoxMatchCost: SKALY.vip_box.matchCost[vb],
    vipBoxSponsorFavorBonus: SKALY.vip_box.sponsorFavor[vb],
    vipBoxSponsorAcceptanceBonus: SKALY.vip_box.sponsorAcceptance[vb],
    vipBoxVillageFavorBonus: SKALY.vip_box.villageFavor[vb],
```

- [ ] **Step 5: Registrovat klíč na serveru**

`apps/api/src/season/finance-processor.ts:422` a `apps/api/src/multiplayer/match-runner.ts:343`: v poli

```ts
["changing_rooms", "showers", "refreshments", "lighting", "stands", "parking", "fence", "roof", "ultras_stand", "toilets", "entrance_gate", "security", "cage"]
```

přidat na konec `, "vip_box"` (obě místa).

`apps/api/src/routes/game.ts`: v obou objektech `const facilities: Record<string, number> = {` (ř. ~1564 v GET `/teams/:teamId/stadium` a ř. ~1901 v POST `/teams/:teamId/stadium/upgrade`) za řádek `    cage: stadium.cage as number ?? 0,` přidat:

```ts
    vip_box: stadium.vip_box as number ?? 0,
```

`apps/api/src/fans/resolve-match-incidents.ts:440-441`: v SELECTu nahradit

```ts
              roof, ultras_stand, toilets, entrance_gate, security, cage
```

za

```ts
              roof, ultras_stand, toilets, entrance_gate, security, cage, vip_box
```

`apps/api/src/news/ultras-report.ts:41`: do `FACILITY_KEYS` přidat na konec `, "vip_box"` a v SELECTu (ř. ~165) nahradit `s.security, s.cage,` za `s.security, s.cage, s.vip_box,` (rubrika počítá kapacitu, tak ať sedí se zápasem).

- [ ] **Step 6: Registrovat klíč na frontendu**

`apps/web/src/app/(hra)/stadion/page.tsx`:
- ve `FACILITY_ICONS` za `  cage: "⛓️",` přidat `  vip_box: "🥂",`
- ve `FACILITY_LABELS` za `  cage: "Klec nad kotlem",` přidat `  vip_box: "VIP lóže",`
- ve `FACILITY_DESCRIPTIONS` za řádek začínající `  cage: ["Mezi kotlem a hřištěm nic není",` přidat:

```ts
  vip_box: ["Hosté sedí mezi diváky", "Prosklená kabina pro pár hostů", "Tři kabiny s rautem a obsluhou", "Celá řada lóží pod markýzou"],
```

`apps/web/src/components/dashboard/widgets/items/fans-widgets.tsx`: ve `FACILITY_LABELS` za `  cage: "Klec",` přidat `  vip_box: "Lóže",`.

- [ ] **Step 7: Spustit testy**

Run: `cd apps/api && npx vitest run src/stadium/vip-box.test.ts src/stadium/stadium-generator.test.ts src/stadium/facility-keys.test.ts`
Expected: PASS, všechny tři soubory zelené.

Run (z kořene repa): `npm run typecheck`
Expected: bez chyb (turbo hlásí úspěch pro `api` i `web`).

- [ ] **Step 8: Aplikovat migraci na testovací DB a ověřit**

Run: `npx wrangler d1 execute prales-db-test --remote --file apps/api/migrations/0218_vip_box.sql`
Expected: `🚣 Executed 1 command` (nebo obdobné hlášení o úspěchu).

Run: `npx wrangler d1 execute prales-db-test --remote --json --command 'SELECT COUNT(*) AS n, MAX(vip_box) AS mx FROM stadiums'`
Expected: `n` > 0, `mx` = 0.

(Wrangler spouštět holý, bez `cd … &&` a bez roury. Když se nenajde konfigurace, spustit se stejným příkazem z adresáře `apps/api` s cestou `migrations/0218_vip_box.sql`.)

- [ ] **Step 9: Commit**

```bash
git add apps/api/migrations/0218_vip_box.sql apps/api/src/stadium/stadium-generator.ts apps/api/src/stadium/vip-box.test.ts apps/api/src/season/finance-processor.ts apps/api/src/multiplayer/match-runner.ts apps/api/src/routes/game.ts apps/api/src/fans/resolve-match-incidents.ts apps/api/src/news/ultras-report.ts "apps/web/src/app/(hra)/stadion/page.tsx" apps/web/src/components/dashboard/widgets/items/fans-widgets.tsx
git commit -m "$(cat <<'EOF'
feat(stadion): VIP lóže v katalogu, kapacita a registrace klíče

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: Provoz lóže ve financích a kapacita ve zbylých výpisech

**Files:**
- Modify: `apps/api/src/season/finance-processor.ts:601-606`
- Modify: `apps/api/src/routes/matches.ts:130-135`
- Modify: `apps/api/src/routes/teams.ts:990-997`, `:1506-1511`, `:3240-3251`
- Create: `apps/api/src/stadium/vip-box-wiring.test.ts`

**Interfaces:**
- Consumes: `StadiumFacilityEffects.vipBoxMatchCost` (úloha 1), `recordTransaction(db, teamId, type, amount, description, gameDate, referenceId?)` z `finance-processor.ts`.
- Produces: transakce typu `match_expense` s popisem `VIP lóže (raut a obsluha)` a `reference_id = matchId` u každého domácího soutěžního zápasu klubu s lóží (liga i pohár, ne přátelák). Existující typ, takže `season/transaction-labels.test.ts` nic nového nepotřebuje.

- [ ] **Step 1: Napsat padající hlídací test**

`apps/api/src/stadium/vip-box-wiring.test.ts`:

```ts
/**
 * Lóže musí dorazit všude, kde se kapacita počítá jen z tribun.
 *
 * `calculateFacilityEffects({ stands })` bez lóže by klubu s lóží ukázal v náhledu
 * zápasu a v profilu víc míst, než kolik jich v zápase opravdu je. Stejný vzor
 * jako `facility-keys.test.ts`: test čte zdroják, protože jde o tichou chybu.
 */
import { describe, expect, it } from "vitest";
import { readFileSync } from "node:fs";
import { join } from "node:path";

const ROOT = join(__dirname, "..");
const zdroj = (rel: string) => readFileSync(join(ROOT, rel), "utf8");

describe("VIP lóže je zapojená", () => {
  for (const soubor of ["routes/matches.ts", "routes/teams.ts"]) {
    it(`${soubor}: každý výpočet kapacity z tribun zná i lóži`, () => {
      const volani = [...zdroj(soubor).matchAll(/\(\{\s*stands:[^}]*\}\)/g)].map((m) => m[0]);
      expect(volani.length).toBeGreaterThan(0);
      expect(volani.filter((v) => !v.includes("vip_box"))).toEqual([]);
    });
  }

  it("finance zápasu strhávají provoz lóže", () => {
    const s = zdroj("season/finance-processor.ts");
    expect(s).toContain("facilityFx.vipBoxMatchCost");
    expect(s).toContain("VIP lóže (raut a obsluha)");
  });
});
```

- [ ] **Step 2: Ověřit, že test padá**

Run: `cd apps/api && npx vitest run src/stadium/vip-box-wiring.test.ts`
Expected: FAIL (4 volání bez `vip_box`, finance bez `vipBoxMatchCost`).

- [ ] **Step 3: Strhnout provoz lóže**

`apps/api/src/season/finance-processor.ts`, uvnitř `if (isHome && attendance > 0) { … if (!isFriendly) { … } }`, hned za blok (anchor):

```ts
      if (facilityFx.securityMatchCost > 0) {
        await recordTransaction(db, teamId, "match_expense", -facilityFx.securityMatchCost,
          `Pořadatelská služba`, gameDate, matchId);
      }
```

přidat:

```ts
      // VIP lóže: raut a obsluha pro pozvané hosty. Platí se za každý domácí
      // soutěžní zápas, i když lóže zůstane prázdná, personál je objednaný
      // dopředu. Stejně jako pořadatelé jde přes `match_expense`, strhne se i v mínusu.
      if (facilityFx.vipBoxMatchCost > 0) {
        await recordTransaction(db, teamId, "match_expense", -facilityFx.vipBoxMatchCost,
          `VIP lóže (raut a obsluha)`, gameDate, matchId);
      }
```

- [ ] **Step 4: Kapacita v náhledu zápasu**

`apps/api/src/routes/matches.ts:130-135`: nahradit

```ts
    "SELECT capacity, stands, pitch_condition, pitch_type, pitch_moisture FROM stadiums WHERE team_id = ?"
  ).bind(homeId).first<{ capacity: number; stands: number | null; pitch_condition: number; pitch_type: string; pitch_moisture: number }>()
```

za

```ts
    "SELECT capacity, stands, vip_box, pitch_condition, pitch_type, pitch_moisture FROM stadiums WHERE team_id = ?"
  ).bind(homeId).first<{ capacity: number; stands: number | null; vip_box: number | null; pitch_condition: number; pitch_type: string; pitch_moisture: number }>()
```

a `calcFxPreview({ stands: stadium.stands ?? 0 })` za `calcFxPreview({ stands: stadium.stands ?? 0, vip_box: stadium.vip_box ?? 0 })`.

- [ ] **Step 5: Kapacita v `teams.ts` (tři místa)**

`:990-997` (GET týmu): SELECT `"SELECT capacity, stands, pitch_condition, pitch_type FROM stadiums WHERE team_id = ? LIMIT 1"` → `"SELECT capacity, stands, vip_box, pitch_condition, pitch_type FROM stadiums WHERE team_id = ? LIMIT 1"`, typ `{ capacity: number; stands: number | null; pitch_condition: number; pitch_type: string }` → `{ capacity: number; stands: number | null; vip_box: number | null; pitch_condition: number; pitch_type: string }`, volání `calcFxTeam({ stands: stadium.stands ?? 0 })` → `calcFxTeam({ stands: stadium.stands ?? 0, vip_box: stadium.vip_box ?? 0 })`.

`:1506-1511` (`/club`): stejná úprava SELECTu a typu, volání `calcFxClub({ stands: stadium.stands ?? 0 })` → `calcFxClub({ stands: stadium.stands ?? 0, vip_box: stadium.vip_box ?? 0 })`.

`:3240-3251`: `"SELECT capacity, stands FROM stadiums WHERE team_id = ?"` → `"SELECT capacity, stands, vip_box FROM stadiums WHERE team_id = ?"`, typ `{ capacity: number; stands: number | null }` → `{ capacity: number; stands: number | null; vip_box: number | null }`, volání `calculateFacilityEffects({ stands: stadiumRow?.stands ?? 0 })` → `calculateFacilityEffects({ stands: stadiumRow?.stands ?? 0, vip_box: stadiumRow?.vip_box ?? 0 })`.

(Pohár `cup/cup.ts:393-395` posílá do `calculateFacilityEffects` celý řádek `SELECT *`, lóži tedy dostane sám; neupravovat.)

- [ ] **Step 6: Spustit testy a typecheck**

Run: `cd apps/api && npx vitest run src/stadium/vip-box-wiring.test.ts src/season/transaction-labels.test.ts src/stadium/facility-keys.test.ts`
Expected: PASS.

Run (kořen): `npm run typecheck`
Expected: bez chyb.

- [ ] **Step 7: Commit**

```bash
git add apps/api/src/season/finance-processor.ts apps/api/src/routes/matches.ts apps/api/src/routes/teams.ts apps/api/src/stadium/vip-box-wiring.test.ts
git commit -m "$(cat <<'EOF'
feat(finance): provoz VIP lóže za domácí zápas, kapacita s lóží v náhledu a profilu

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: Majitelé firem v lóži

**Files:**
- Modify: `apps/api/src/sponsors/favor-math.ts`
- Modify: `apps/api/src/sponsors/favor-math.test.ts`
- Modify: `apps/api/src/sponsors/hooks.ts:10` a `:21-41`
- Create: `apps/api/src/sponsors/hooks.test.ts`
- Modify: `apps/api/src/multiplayer/match-runner.ts:823-825`
- Modify: `apps/api/src/routes/sponsors.ts:228-230`

**Interfaces:**
- Consumes: `SKALY.vip_box` (úloha 1), `favorDeltaStmt(db, sponsorId, teamId, delta)` z `sponsors/favor.ts` (bind: `sponsorId, teamId, DEFAULT_FAVOR, delta, delta`), `FalesnaD1`/`jakoD1` z `apps/api/src/incidents/testovaci-d1.ts`.
- Produces:
  - `vipBoxFavorBonus(level: number): number` (0–3 → 0/1/2/3, mimo rozsah se ořízne)
  - `vipBoxAcceptanceBonus(level: number): number` (0–3 → 0/0,05/0,10/0,15)
  - `invitationAcceptance(i: { favor: number; personality: OwnerPersonality; recentLosses: number; noise: number; vipBoxLevel?: number }): number`
  - `settleSponsorInvitations(db: D1Database, matchId: string, homeTeamId: string, homeScore: number, awayScore: number, vipBoxLevel?: number): Promise<void>` (výchozí `vipBoxLevel = 0`)

- [ ] **Step 1: Padající testy čisté matematiky**

`apps/api/src/sponsors/favor-math.test.ts`: v importu `from "./favor-math"` přidat `vipBoxAcceptanceBonus, vipBoxFavorBonus` a na konec `describe("favor-math", …)` (před poslední `});`) přidat:

```ts
  it("VIP lóže: majitel po zápase +1/+2/+3, mimo rozsah se ořízne", () => {
    expect([0, 1, 2, 3].map(vipBoxFavorBonus)).toEqual([0, 1, 2, 3]);
    expect(vipBoxFavorBonus(7)).toBe(3);
    expect(vipBoxFavorBonus(-1)).toBe(0);
  });

  it("VIP lóže: šance na přijetí +5/+10/+15 p. b., strop 95 % platí dál", () => {
    expect([0, 1, 2, 3].map(vipBoxAcceptanceBonus)).toEqual([0, 0.05, 0.1, 0.15]);
    const bez = invitationAcceptance({ favor: 50, personality: "businessman", recentLosses: 0, noise: 0 });
    const s = invitationAcceptance({ favor: 50, personality: "businessman", recentLosses: 0, noise: 0, vipBoxLevel: 3 });
    expect(bez).toBeCloseTo(0.4, 5);
    expect(s).toBeCloseTo(0.55, 5);
    expect(invitationAcceptance({ favor: 100, personality: "patriot", recentLosses: 0, noise: 0.1, vipBoxLevel: 3 })).toBe(0.95);
  });
```

Run: `cd apps/api && npx vitest run src/sponsors/favor-math.test.ts`
Expected: FAIL (`vipBoxFavorBonus` není exportováno).

- [ ] **Step 2: Implementace ve `favor-math.ts`**

Za řádek `import type { OwnerPersonality } from "./owners";` přidat:

```ts
import { SKALY } from "../stadium/stadium-generator";
```

Nad `export function invitationAcceptance` přidat:

```ts
/** Úroveň lóže 0–3 z čehokoli, co přijde z DB (null, desetinné, mimo rozsah). */
function vipLevel(level: number): number {
  return Math.max(0, Math.min(3, Math.round(level || 0)));
}

/** VIP lóže: o kolik náklonnosti navíc majitel po zápase přidá, když seděl v lóži. */
export function vipBoxFavorBonus(level: number): number {
  return SKALY.vip_box.sponsorFavor[vipLevel(level)];
}

/** VIP lóže: o kolik (podíl 0–1) vzroste šance, že majitel pozvání přijme. */
export function vipBoxAcceptanceBonus(level: number): number {
  return SKALY.vip_box.sponsorAcceptance[vipLevel(level)];
}
```

Upravit `invitationAcceptance`: v typu parametru `favor: number; personality: OwnerPersonality; recentLosses: number; noise: number;` přidat `vipBoxLevel?: number;` a řádek `  p += i.noise;` nahradit:

```ts
  // Pozvání do lóže je lákavější než lavička na tribuně.
  p += vipBoxAcceptanceBonus(i.vipBoxLevel ?? 0);
  p += i.noise;
```

Run: `cd apps/api && npx vitest run src/sponsors/favor-math.test.ts`
Expected: PASS.

- [ ] **Step 3: Padající test vyhodnocení pozvánek**

`apps/api/src/sponsors/hooks.test.ts`:

```ts
/**
 * Vyhodnocení pozvánek majitelů po zápase: bonus za VIP lóži a jednorázovost.
 */
import { describe, expect, it } from "vitest";
import { FalesnaD1, jakoD1 } from "../incidents/testovaci-d1";
import { settleSponsorInvitations } from "./hooks";

const POZVANKA = { sql: /FROM sponsor_invitations si/, all: [{ id: "i1", sponsor_id: 7, personality: "businessman" }] };

function zapsanaDelta(db: FalesnaD1): unknown {
  return db.dotazy.find((d) => /INSERT INTO sponsor_team_favor/.test(d.sql))?.params[3];
}

describe("settleSponsorInvitations", () => {
  it("výhra bez lóže: +4", async () => {
    const db = new FalesnaD1([POZVANKA, { sql: /UPDATE sponsor_invitations SET status = 'attended'/, changes: 1 }]);
    await settleSponsorInvitations(jakoD1(db), "m1", "t1", 2, 1);
    expect(zapsanaDelta(db)).toBe(4);
  });

  it("výhra s lóží L2: +4 a +2 za lóži", async () => {
    const db = new FalesnaD1([POZVANKA, { sql: /UPDATE sponsor_invitations SET status = 'attended'/, changes: 1 }]);
    await settleSponsorInvitations(jakoD1(db), "m1", "t1", 2, 1, 2);
    expect(zapsanaDelta(db)).toBe(6);
  });

  it("prohra s lóží L3: −1 a +3 za lóži", async () => {
    const db = new FalesnaD1([POZVANKA, { sql: /UPDATE sponsor_invitations SET status = 'attended'/, changes: 1 }]);
    await settleSponsorInvitations(jakoD1(db), "m1", "t1", 0, 1, 3);
    expect(zapsanaDelta(db)).toBe(2);
  });

  it("pozvánku už vyhodnotil jiný běh: nic se nepřičte", async () => {
    const db = new FalesnaD1([POZVANKA, { sql: /UPDATE sponsor_invitations SET status = 'attended'/, changes: 0 }]);
    await settleSponsorInvitations(jakoD1(db), "m1", "t1", 2, 1, 3);
    expect(db.pocet(/INSERT INTO sponsor_team_favor/)).toBe(0);
  });
});
```

Run: `cd apps/api && npx vitest run src/sponsors/hooks.test.ts`
Expected: FAIL v testech s lóží (delta 4 místo 6, −1 místo 2); první a poslední test projdou.

- [ ] **Step 4: Implementace v `hooks.ts`**

Import na ř. 10 nahradit:

```ts
import { DEFAULT_FAVOR, postMatchFavorDelta, riotFavorDelta, SEASON_PARTNERSHIP_FAVOR, vipBoxFavorBonus } from "./favor-math";
```

Signaturu a výpočet v `settleSponsorInvitations` upravit takto (celá funkce):

```ts
export async function settleSponsorInvitations(
  db: D1Database, matchId: string, homeTeamId: string, homeScore: number, awayScore: number,
  vipBoxLevel = 0,
): Promise<void> {
  const rows = await db.prepare(
    `SELECT si.id, si.sponsor_id, so.personality FROM sponsor_invitations si
     LEFT JOIN sponsor_owners so ON so.sponsor_id = si.sponsor_id
     WHERE si.match_id = ? AND si.team_id = ? AND si.status = 'accepted'`,
  ).bind(matchId, homeTeamId).all<{ id: string; sponsor_id: number; personality: string | null }>();
  if (rows.results.length === 0) return;
  // Majitel pozvaný na domácí zápas sedí v lóži, pokud ji klub má. Výsledek
  // mu pohne náklonností jako dřív, lóže k tomu přidá pevný bonus.
  const vipBonus = vipBoxFavorBonus(vipBoxLevel);
  let claimed = 0;
  for (const r of rows.results) {
    const claim = await db.prepare(
      "UPDATE sponsor_invitations SET status = 'attended' WHERE id = ? AND status = 'accepted'",
    ).bind(r.id).run();
    if ((claim.meta?.changes ?? 0) !== 1) continue;
    claimed++;
    const p: OwnerPersonality = isOwnerPersonality(r.personality) ? r.personality : "businessman";
    await favorDeltaStmt(db, r.sponsor_id, homeTeamId, postMatchFavorDelta(p, homeScore, awayScore) + vipBonus).run();
  }
  logger.info({ module: "sponsors", matchId }, `majitelé na tribuně: ${claimed}, bonus lóže ${vipBonus}`);
}
```

Docstring nad funkcí doplnit o větu: `VIP lóže (úroveň 0–3) přidá každému přítomnému majiteli vipBoxFavorBonus.`

- [ ] **Step 5: Předat úroveň lóže z match-runneru**

`apps/api/src/multiplayer/match-runner.ts:825` (za zámkem zápasu, `facilities` je ve stejném bloku definované na ř. ~340 a od úlohy 1 obsahuje `vip_box`): nahradit

```ts
                await settleSponsorInvitations(db, matchId, homeTeamId, result.homeScore, result.awayScore);
```

za

```ts
                await settleSponsorInvitations(db, matchId, homeTeamId, result.homeScore, result.awayScore, facilities.vip_box ?? 0);
```

- [ ] **Step 6: Šance na přijetí v invite routě**

`apps/api/src/routes/sponsors.ts`, v handleru `sponsorsRouter.post("/teams/:teamId/sponsor-owners/:sponsorId/invite", …)` těsně před (anchor):

```ts
  const probability = invitationAcceptance({
    favor, personality: owner.personality, recentLosses, noise: (Math.random() - 0.5) * 0.2,
  });
```

přidat:

```ts
  // Lóže dělá z pozvání zážitek. Když se úroveň nenačte, zve se jako bez lóže.
  const vipRow = await db.prepare("SELECT vip_box FROM stadiums WHERE team_id = ?")
    .bind(teamId).first<{ vip_box: number | null }>()
    .catch((e) => { logger.warn({ module: "sponsors", teamId }, "load vip box for invite", e); return null; });
```

a volání nahradit:

```ts
  const probability = invitationAcceptance({
    favor, personality: owner.personality, recentLosses, noise: (Math.random() - 0.5) * 0.2,
    vipBoxLevel: vipRow?.vip_box ?? 0,
  });
```

- [ ] **Step 7: Testy a typecheck**

Run: `cd apps/api && npx vitest run src/sponsors/`
Expected: PASS (favor-math, hooks, owners, budget).

Run (kořen): `npm run typecheck`
Expected: bez chyb.

- [ ] **Step 8: Commit**

```bash
git add apps/api/src/sponsors/favor-math.ts apps/api/src/sponsors/favor-math.test.ts apps/api/src/sponsors/hooks.ts apps/api/src/sponsors/hooks.test.ts apps/api/src/multiplayer/match-runner.ts apps/api/src/routes/sponsors.ts
git commit -m "$(cat <<'EOF'
feat(sponzori): VIP lóže zvedá šanci na přijetí pozvání a náklonnost majitele po zápase

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: Zastupitelé obce v lóži

**Files:**
- Create: `apps/api/src/villages/vip-box-favor.ts`
- Create: `apps/api/src/villages/vip-box-favor.test.ts`
- Modify: `apps/api/src/multiplayer/match-runner.ts` (hned za blok `settleSponsorInvitations`, ř. ~826-828)

**Interfaces:**
- Consumes: `calculateFacilityEffects(...).vipBoxVillageFavorBonus` (úloha 1); tabulky `village_invitations (match_id, team_id, official_id, status)`, `village_officials (id, village_id)`, `village_team_favor (id, village_id, team_id, official_id, favor, trust, last_interaction_at, updated_at)`; výchozí osobní přízeň 50 (stejně jako brigády v `routes/villages.ts`).
- Produces: `applyVipBoxVillageFavor(db: D1Database, matchId: string, homeTeamId: string, vipBoxLevel: number, now?: string): Promise<number>` vrací počet zastupitelů, kterým přízeň přibyla.

Idempotence: funkce se volá jen za zámkem zápasu (`UPDATE matches … WHERE status = 'lineups_open'` s kontrolou `changes`), kterým projde jediný běh. Pozvánky se na `attended` přepínají už před simulací (ř. ~717-728), takže za zámkem je stav čitelný a druhý souběžný běh sem nikdy nedojde.

- [ ] **Step 1: Padající test**

`apps/api/src/villages/vip-box-favor.test.ts`:

```ts
/**
 * Přízeň zastupitelů, kteří zápas prosedí ve VIP lóži.
 */
import { describe, expect, it } from "vitest";
import { FalesnaD1, jakoD1 } from "../incidents/testovaci-d1";
import { applyVipBoxVillageFavor } from "./vip-box-favor";

const NOW = "2026-09-22T10:00:00.000Z";
const PRITOMNI = {
  sql: /FROM village_invitations vi/,
  all: [{ official_id: "o1", village_id: "v1" }, { official_id: "o2", village_id: "v1" }],
};

describe("applyVipBoxVillageFavor", () => {
  it("bez lóže se nic nečte ani nezapisuje", async () => {
    const db = new FalesnaD1([PRITOMNI]);
    expect(await applyVipBoxVillageFavor(jakoD1(db), "m1", "t1", 0, NOW)).toBe(0);
    expect(db.dotazy).toEqual([]);
  });

  it("L3: každý přítomný zastupitel s řádkem přízně dostane +3", async () => {
    const db = new FalesnaD1([PRITOMNI, { sql: /SELECT id FROM village_team_favor/, first: { id: "f1" } }]);
    expect(await applyVipBoxVillageFavor(jakoD1(db), "m1", "t1", 3, NOW)).toBe(2);
    const updaty = db.dotazy.filter((d) => /UPDATE village_team_favor/.test(d.sql));
    expect(updaty).toHaveLength(2);
    expect(updaty[0].params).toEqual([3, NOW, NOW, "f1"]);
  });

  it("chybějící osobní řádek se založí z 50 + bonus", async () => {
    const db = new FalesnaD1([PRITOMNI]);
    await applyVipBoxVillageFavor(jakoD1(db), "m1", "t1", 1, NOW);
    const inserty = db.dotazy.filter((d) => /INSERT INTO village_team_favor/.test(d.sql));
    expect(inserty).toHaveLength(2);
    expect(inserty[0].params.slice(1)).toEqual(["v1", "t1", "o1", 51, NOW, NOW]);
  });

  it("čte jen zastupitele, kteří na zápase opravdu byli", async () => {
    const db = new FalesnaD1([PRITOMNI]);
    await applyVipBoxVillageFavor(jakoD1(db), "m1", "t1", 2, NOW);
    const dotaz = db.dotazy.find((d) => /FROM village_invitations vi/.test(d.sql));
    expect(dotaz?.sql).toContain("vi.status = 'attended'");
    expect(dotaz?.params).toEqual(["m1", "t1"]);
  });
});
```

Run: `cd apps/api && npx vitest run src/villages/vip-box-favor.test.ts`
Expected: FAIL (modul neexistuje).

- [ ] **Step 2: Implementace**

`apps/api/src/villages/vip-box-favor.ts`:

```ts
/**
 * VIP lóže a zastupitelé obce.
 *
 * Kdo z pozvaných zastupitelů zápas opravdu prosedí (pozvánka `attended`),
 * tomu přibude osobní přízeň podle úrovně lóže. Volá se z match-runneru až
 * za zámkem zápasu, takže se za jeden zápas připíše nejvýš jednou.
 */
import { logger } from "../lib/logger";
import { calculateFacilityEffects } from "../stadium/stadium-generator";

export async function applyVipBoxVillageFavor(
  db: D1Database,
  matchId: string,
  homeTeamId: string,
  vipBoxLevel: number,
  now: string = new Date().toISOString(),
): Promise<number> {
  const bonus = calculateFacilityEffects({ vip_box: vipBoxLevel }).vipBoxVillageFavorBonus;
  if (bonus <= 0) return 0;

  const rows = await db.prepare(
    `SELECT vi.official_id, vo.village_id
     FROM village_invitations vi
     JOIN village_officials vo ON vo.id = vi.official_id
     WHERE vi.match_id = ? AND vi.team_id = ? AND vi.status = 'attended'`,
  ).bind(matchId, homeTeamId).all<{ official_id: string; village_id: string }>();

  let applied = 0;
  for (const r of rows.results) {
    try {
      const existing = await db.prepare(
        "SELECT id FROM village_team_favor WHERE team_id = ? AND official_id = ?",
      ).bind(homeTeamId, r.official_id).first<{ id: string }>();
      if (existing) {
        await db.prepare(
          `UPDATE village_team_favor
           SET favor = MIN(100, favor + ?), last_interaction_at = ?, updated_at = ?
           WHERE id = ?`,
        ).bind(bonus, now, now, existing.id).run();
      } else {
        // Osobní řádek se zakládá líně z výchozích 50, stejně jako u brigád.
        await db.prepare(
          `INSERT INTO village_team_favor (id, village_id, team_id, official_id, favor, trust, last_interaction_at, updated_at)
           VALUES (?, ?, ?, ?, ?, 50, ?, ?)`,
        ).bind(crypto.randomUUID(), r.village_id, homeTeamId, r.official_id, Math.min(100, 50 + bonus), now, now).run();
      }
      applied++;
    } catch (e) {
      logger.warn({ module: "villages", matchId, teamId: homeTeamId }, `přízeň zastupitele ${r.official_id} z VIP lóže`, e);
    }
  }
  logger.info({ module: "villages", matchId, teamId: homeTeamId }, `VIP lóže: zastupitelů v lóži ${applied}, +${bonus} přízně`);
  return applied;
}
```

Run: `cd apps/api && npx vitest run src/villages/vip-box-favor.test.ts`
Expected: PASS.

- [ ] **Step 3: Zapojit do match-runneru**

`apps/api/src/multiplayer/match-runner.ts`: hned za blok (anchor, po úloze 3):

```ts
            try {
                const { settleSponsorInvitations } = await import("../sponsors/hooks");
                await settleSponsorInvitations(db, matchId, homeTeamId, result.homeScore, result.awayScore, facilities.vip_box ?? 0);
            } catch (e) {
                logger.warn({module: "match-runner", matchId}, "settle sponsor invitations", e);
            }
```

přidat:

```ts

            // Zastupitelé obce ve VIP lóži. Až za zámkem zápasu: pozvánky jsou už
            // `attended` (přepnuté před simulací) a sem dojde jen jeden běh.
            if ((facilities.vip_box ?? 0) > 0 && officialCount > 0) {
                try {
                    const { applyVipBoxVillageFavor } = await import("../villages/vip-box-favor");
                    await applyVipBoxVillageFavor(db, matchId, homeTeamId, facilities.vip_box ?? 0);
                } catch (e) {
                    logger.warn({module: "match-runner", matchId}, "VIP lóže: přízeň zastupitelů", e);
                }
            }
```

(`officialCount` je definovaný na ř. ~628 ve stejném bloku.)

- [ ] **Step 4: Testy a typecheck**

Run: `cd apps/api && npx vitest run src/villages/ src/sponsors/ src/stadium/`
Expected: PASS.

Run (kořen): `npm run typecheck`
Expected: bez chyb.

- [ ] **Step 5: Commit**

```bash
git add apps/api/src/villages/vip-box-favor.ts apps/api/src/villages/vip-box-favor.test.ts apps/api/src/multiplayer/match-runner.ts
git commit -m "$(cat <<'EOF'
feat(obec): zastupitelé ve VIP lóži získají přízeň za domácí zápas

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: Stránka stadionu a 2D náhled

**Files:**
- Modify: `apps/api/src/routes/game.ts:1663-1665` a response ř. ~1725
- Modify: `apps/web/src/app/(hra)/stadion/page.tsx` (interface `StadiumData` ř. ~108, karta zařízení ř. ~1117-1136)
- Modify: `apps/web/src/components/stadium/stadium-view.tsx:27-36` a `:57`

**Interfaces:**
- Consumes: `calculateFacilityEffects` pole `vipBox*` (úloha 1).
- Produces: v odpovědi `GET /api/teams/:teamId/stadium` nové pole
  `vipBox: { level: number; seatsLost: number; matchCost: number; sponsorFavorBonus: number; sponsorAcceptancePct: number; villageFavorBonus: number }`.

- [ ] **Step 1: Shrnutí lóže v GET stadionu**

`apps/api/src/routes/game.ts`, za řádek (anchor):

```ts
  const effectiveCapacity = ((stadium.capacity as number) ?? 200) + calculateFacilityEffects(facilities).capacityBonus;
```

přidat:

```ts
  // Co lóže právě dává a bere. Stránka stadionu to vypíše u karty lóže, aby
  // hráč po nákupu nejvyšší úrovně (kdy nabídka upgradu zmizí) věděl, co má.
  const vipFx = calculateFacilityEffects(facilities);
  const vipBox = {
    level: facilities.vip_box ?? 0,
    seatsLost: vipFx.vipBoxCapacityLoss,
    matchCost: vipFx.vipBoxMatchCost,
    sponsorFavorBonus: vipFx.vipBoxSponsorFavorBonus,
    sponsorAcceptancePct: Math.round(vipFx.vipBoxSponsorAcceptanceBonus * 100),
    villageFavorBonus: vipFx.vipBoxVillageFavorBonus,
  };
```

a v `return c.json({ … })` za řádek `    facilities,` (ten nad `    customization,`) přidat `    vipBox,`.

- [ ] **Step 2: Typ a řádek „Teď:" na stránce stadionu**

`apps/web/src/app/(hra)/stadion/page.tsx`, v `interface StadiumData` za řádek `  facilities: Record<string, number>;` přidat:

```ts
  /** Co VIP lóže právě dává a bere (z API, počítá se ze stejné škály jako zápas). */
  vipBox?: {
    level: number;
    seatsLost: number;
    matchCost: number;
    sponsorFavorBonus: number;
    sponsorAcceptancePct: number;
    villageFavorBonus: number;
  };
```

V kartě zařízení (smyčka `Object.entries(stadium.facilities).map(([key, level]) => {`) těsně před komentář `              {/* Upgrade info + action */}` přidat:

```tsx
              {key === "vip_box" && level > 0 && stadium.vipBox && (
                <div className="text-sm text-muted leading-snug mb-2">
                  Teď: o {stadium.vipBox.seatsLost} míst méně pro platící diváky, provoz{" "}
                  <span className="tabular-nums">{formatCZK(stadium.vipBox.matchCost)}</span> za domácí zápas.
                  Pozvaný majitel přijme o {stadium.vipBox.sponsorAcceptancePct} % častěji a po zápase přidá
                  +{stadium.vipBox.sponsorFavorBonus} náklonnosti, každý zastupitel v lóži +{stadium.vipBox.villageFavorBonus} přízně.
                </div>
              )}
```

- [ ] **Step 3: Čip ve 2D náhledu**

`apps/web/src/components/stadium/stadium-view.tsx`: ve `FACILITY_CONFIG` za `  security: { icon: "\u{1F9BA}", label: "Pořadatelská služba" },` přidat:

```ts
  vip_box: { icon: "\u{1F942}", label: "VIP lóže" },
```

a řádek

```ts
  const topChips = ["lighting", "fence", "entrance_gate"].filter((k) => f[k] > 0);
```

nahradit

```ts
  const topChips = ["lighting", "fence", "entrance_gate", "vip_box"].filter((k) => f[k] > 0);
```

(Test `facility-keys.test.ts` kontroluje, že každý klíč v seznamu čipů má záznam ve `FACILITY_CONFIG`.)

- [ ] **Step 4: Testy, typecheck, build**

Run: `cd apps/api && npx vitest run src/stadium/facility-keys.test.ts`
Expected: PASS.

Run (kořen): `npm run typecheck`
Expected: bez chyb.

Run: `cd apps/web && npx next build --no-lint`
Expected: `✓ Compiled successfully`, build doběhne bez chyb.

- [ ] **Step 5: Commit**

```bash
git add apps/api/src/routes/game.ts "apps/web/src/app/(hra)/stadion/page.tsx" apps/web/src/components/stadium/stadium-view.tsx
git commit -m "$(cat <<'EOF'
feat(stadion): přehled VIP lóže na stránce stadionu a čip ve 2D náhledu

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: VIP lóže ve 3D

**Files:**
- Create: `apps/web/src/components/stadium/stadium-3d/VipBox.tsx`
- Modify: `apps/web/src/components/stadium/stadium-3d/Stadium3D.tsx` (import ř. ~16, vykreslení za `<StandRoof … />` ř. ~458)
- Modify: `apps/web/src/components/stadium/stadium-3d/Stand.tsx` (blok „4. VIP skybox" ř. ~176-185 a funkce `VIPBox` ř. ~808-863)

**Interfaces:**
- Consumes: `PITCH`, `STAND_DIMS`, `type TimeOfDay` z `./constants`; v `Stadium3D` proměnné `f` (facilities), `SEKTOR_STRANY`, `ultrasSector`, `accentColor`, `teamColor`, `timeOfDay`, `isMobile`.
- Produces:
  - `vipBoxSide(standsLevel: number, ultrasSide: "north" | "south" | "east" | "west"): "north" | "south" | "east" | "west"`
  - `VipBox(props: { level: number; standsLevel: number; ultrasSide: Side; accentColor: string; teamColor: string; timeOfDay: TimeOfDay; reducedDetail?: boolean })`

Geometrie: tribuna má lokální souřadnice od hrany hřiště (`z = 0`, přední řada) dozadu (`z = dims.depth`), výška `dims.height`, skupina je posunutá a otočená stejně jako v `Stand.tsx` (`north` = `[0, 0, distance]`, rotace 0 atd.). Lóže stojí ZA zadní hranou tribuny na sloupech, dno ve výšce `dims.height`, 1,2 m za zády tribuny, aby nekolidovala se zadními sloupky střechy (`StandRoof` je má nejdál v `depth + 0,775`). K tribuně ji spojuje lávka. Hlavní tribuna je východní (`SEKTOR_STRANY.hlavni`); tribuny L1 mají jen sever a jih, tam lóže sedí na severu (za brankou). Kde stojí kotel, lóže nebude, přesune se na protější stranu.

- [ ] **Step 1: Komponenta `VipBox.tsx`**

```tsx
"use client";
import { PITCH, STAND_DIMS, type TimeOfDay } from "./constants";

/** Stejná mezera mezi hřištěm a tribunou jako v Stand.tsx a StadiumExtras.tsx. */
const STAND_GAP = 2.5;
/** Odstup lóže od zad tribuny, ať se nepotká se zadními sloupky střechy. */
const BACK_OFFSET = 1.2;

type Side = "north" | "south" | "east" | "west";

/** Počet kabin podle úrovně. L3 je souvislá řada přes většinu tribuny. */
const CABINS = [0, 1, 3, 6] as const;
const CABIN_W = 3.2;
const CABIN_H = 1.9;
const CABIN_D = 2.2;

/**
 * Na které tribuně lóže stojí. Hlavní je východní podélná tribuna, ta ale
 * existuje až od tribun L2; do té doby lóže sedí na severu za brankou.
 * Kde stojí kotel, tam lóže nepatří, proto se přesune naproti.
 */
export function vipBoxSide(standsLevel: number, ultrasSide: Side): Side {
  const preferred: Side = standsLevel >= 2 ? "east" : "north";
  if (preferred !== ultrasSide) return preferred;
  return preferred === "east" ? "west" : "south";
}

interface VipBoxProps {
  level: number;
  standsLevel: number;
  ultrasSide: Side;
  accentColor: string;
  teamColor: string;
  timeOfDay: TimeOfDay;
  reducedDetail?: boolean;
}

export function VipBox(props: VipBoxProps) {
  if (props.level <= 0 || props.standsLevel <= 0) return null;
  return <ActiveVipBox {...props} />;
}

function ActiveVipBox({ level, standsLevel, ultrasSide, accentColor, teamColor, timeOfDay, reducedDetail = false }: VipBoxProps) {
  const lv = Math.min(level, 3);
  const dims = STAND_DIMS[Math.min(standsLevel, 3)];
  const side = vipBoxSide(standsLevel, ultrasSide);
  const isEW = side === "east" || side === "west";
  const standLength = isEW ? PITCH.depth : PITCH.width;
  // Stejné umístění skupiny jako Stand: lokální +Z míří od hřiště dozadu.
  const distance = (isEW ? PITCH.width : PITCH.depth) / 2 + STAND_GAP + dims.depth / 2;
  const position: [number, number, number] =
    side === "north" ? [0, 0, distance]
    : side === "south" ? [0, 0, -distance]
    : side === "east" ? [distance, 0, 0]
    : [-distance, 0, 0];
  const rotationY =
    side === "north" ? 0 : side === "south" ? Math.PI : side === "east" ? Math.PI / 2 : -Math.PI / 2;

  const cabins = CABINS[lv];
  // L3: řada přes 80 % délky tribuny, kabiny se roztáhnou; nižší úrovně mají pevnou šířku.
  const cabinW = lv >= 3 ? (standLength * 0.8) / cabins : CABIN_W;
  const rowW = cabinW * cabins;
  const baseY = dims.height;
  const z = dims.depth + BACK_OFFSET + CABIN_D / 2;
  // Večer a v noci lóže svítí, přes den je sklo jen tónované.
  const glow = timeOfDay === "day" ? 0.25 : 0.9;
  const postXs = lv >= 3 && !reducedDetail
    ? [-rowW / 2 + 0.2, -rowW / 4, 0, rowW / 4, rowW / 2 - 0.2]
    : [-rowW / 2 + 0.2, rowW / 2 - 0.2];

  return (
    <group position={position} rotation={[0, rotationY, 0]}>
      {/* Sloupy pod lóží */}
      {postXs.map((x, i) => (
        <mesh key={`post-${i}`} position={[x, baseY / 2, z]} castShadow>
          <boxGeometry args={[0.25, baseY, 0.25]} />
          <meshStandardMaterial color="#4B5563" metalness={0.4} roughness={0.6} />
        </mesh>
      ))}

      {/* Lávka ze zad tribuny do lóže */}
      <mesh position={[0, baseY - 0.06, dims.depth + BACK_OFFSET / 2]} receiveShadow>
        <boxGeometry args={[Math.min(rowW, 3), 0.12, BACK_OFFSET]} />
        <meshStandardMaterial color="#6B7280" roughness={0.8} />
      </mesh>

      {/* Podlaha a zadní stěna celé řady */}
      <mesh position={[0, baseY + 0.08, z]} castShadow receiveShadow>
        <boxGeometry args={[rowW + 0.3, 0.16, CABIN_D + 0.2]} />
        <meshStandardMaterial color="#374151" roughness={0.7} />
      </mesh>
      <mesh position={[0, baseY + CABIN_H / 2, z + CABIN_D / 2]} castShadow>
        <boxGeometry args={[rowW + 0.3, CABIN_H, 0.15]} />
        <meshStandardMaterial color="#1F2937" roughness={0.6} />
      </mesh>

      {/* Kabiny: tmavý rám, sklo do hřiště, příčky mezi kabinami */}
      {Array.from({ length: cabins }).map((_, i) => {
        const x = -rowW / 2 + cabinW * (i + 0.5);
        return (
          <group key={`cab-${i}`} position={[x, baseY + 0.16, z]}>
            {/* Strop kabiny */}
            <mesh position={[0, CABIN_H - 0.06, 0]} castShadow>
              <boxGeometry args={[cabinW, 0.12, CABIN_D]} />
              <meshStandardMaterial color="#111827" metalness={0.6} roughness={0.35} />
            </mesh>
            {/* Čelní sklo. Rovina se otáčí k hřišti (lokální −Z). */}
            <mesh position={[0, CABIN_H / 2, -CABIN_D / 2]} rotation={[0, Math.PI, 0]}>
              <planeGeometry args={[cabinW - 0.12, CABIN_H - 0.2]} />
              <meshStandardMaterial
                color="#60A5FA"
                emissive="#FDE68A"
                emissiveIntensity={glow}
                metalness={0.85}
                roughness={0.1}
                transparent
                opacity={0.8}
              />
            </mesh>
            {/* Boční příčka (sloupek rámu) */}
            <mesh position={[cabinW / 2 - 0.04, CABIN_H / 2, 0]}>
              <boxGeometry args={[0.08, CABIN_H, CABIN_D]} />
              <meshStandardMaterial color="#111827" metalness={0.6} roughness={0.35} />
            </mesh>
          </group>
        );
      })}
      {/* Levý krajní sloupek rámu (pravý kreslí poslední kabina) */}
      <mesh position={[-rowW / 2 + 0.04, baseY + 0.16 + CABIN_H / 2, z]}>
        <boxGeometry args={[0.08, CABIN_H, CABIN_D]} />
        <meshStandardMaterial color="#111827" metalness={0.6} roughness={0.35} />
      </mesh>

      {/* Klubový lem nad sklem */}
      <mesh position={[0, baseY + 0.16 + CABIN_H + 0.06, z - CABIN_D / 2]} castShadow>
        <boxGeometry args={[rowW + 0.3, 0.14, 0.14]} />
        <meshStandardMaterial color={accentColor} emissive={accentColor} emissiveIntensity={0.3} metalness={0.6} roughness={0.35} />
      </mesh>

      {/* L3: markýza v klubové barvě přes celou řadu */}
      {lv >= 3 && (
        <mesh position={[0, baseY + 0.16 + CABIN_H + 0.35, z - 0.5]} rotation={[-0.22, 0, 0]} castShadow>
          <boxGeometry args={[rowW + 0.8, 0.1, CABIN_D + 1.4]} />
          <meshStandardMaterial color={teamColor} roughness={0.6} metalness={0.1} />
        </mesh>
      )}
    </group>
  );
}
```

- [ ] **Step 2: Vykreslit v `Stadium3D.tsx`**

Za řádek `import { HostujiciSektor, StandRoof, UltrasSector } from "./StadiumExtras";` přidat:

```tsx
import { VipBox } from "./VipBox";
```

Za řádek (anchor):

```tsx
          <StandRoof standsLevel={f.stands ?? 0} roofLevel={f.roof ?? 0} roofColor={roofColor} weather={weather} />
```

přidat:

```tsx

          {/* VIP lóže za hlavní tribunou (bez tribuny se nekreslí) */}
          <VipBox
            level={f.vip_box ?? 0}
            standsLevel={f.stands ?? 0}
            ultrasSide={SEKTOR_STRANY[ultrasSector]}
            accentColor={accentColor}
            teamColor={teamColor}
            timeOfDay={timeOfDay}
            reducedDetail={isMobile}
          />
```

- [ ] **Step 3: Zrušit dekorativní skybox u tribun L3**

`apps/web/src/components/stadium/stadium-3d/Stand.tsx`: smazat blok

```tsx
      {/* 4. VIP skybox a novinářská lávka (L3). Střecha je samostatné zařízení. */}
      {level >= 3 && (
        <VIPBox
          length={length}
          height={dims.height}
          depth={dims.depth}
          accentColor={finalAccent}
          teamColor={teamColor}
        />
      )}
```

a celou funkci `function VIPBox({ … }) { … }` včetně komentáře `/** Prosklené VIP skyboxy pro L3 tribunu */` (končí těsně před `/** Vstupní tunel pro nástup hráčů */`). Lóže je teď samostatné zařízení (`VipBox.tsx`). `finalAccent` (ř. ~97, `const finalAccent = accentColor ?? "#C9A84C";`) se používal jen pro skybox, takže smazat i ten řádek. Prop `accentColor` v `StandProps` zůstává (Stadium3D ho posílá dál), jen se v `ActiveStand` přestane číst.

- [ ] **Step 4: Typecheck a build**

Run (kořen): `npm run typecheck`
Expected: bez chyb.

Run: `cd apps/web && npx next build --no-lint`
Expected: `✓ Compiled successfully`.

- [ ] **Step 5: Vizuální kontrola lokálně**

Spustit `npm run dev` (FE musí volat `localhost:8787`, viz `.env.local`). V lokální D1 nastavit testovacímu týmu tribuny a lóži (lokální sqlite, smí se):

```bash
npx wrangler d1 execute prales-db-test --local --command 'UPDATE stadiums SET stands = 2, vip_box = 3 WHERE team_id = "302a0ce7-428a-4da8-b4ac-40f27eb9a7d1"'
```

(Spouštět z `apps/api`, holý příkaz.) Přes MCP prohlížeč otevřít `http://localhost:3000/stadion`, 3D náhled, pohled „overview": řada lóží s markýzou za východní tribunou, sklo směrem do hřiště, nic neprochází střechou ani sloupky. Pak `vip_box = 1` a `stands = 1`: jedna kabina za severní tribunou. Přepnout na večer: sklo svítí. Screenshot obou stavů. Když lóže koliduje se střechou (`roof` > 0), zvětšit `BACK_OFFSET` po 0,3 m, dokud nekoliduje.

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/components/stadium/stadium-3d/VipBox.tsx apps/web/src/components/stadium/stadium-3d/Stadium3D.tsx apps/web/src/components/stadium/stadium-3d/Stand.tsx
git commit -m "$(cat <<'EOF'
feat(3d): VIP lóže za hlavní tribunou, roste s úrovní; pryč dekorativní skybox u tribun L3

Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: Nasazení na testing a ověření

**Files:** žádné změny kódu.

**Interfaces:**
- Consumes: vše z úloh 1–6; migrace 0218 aplikovaná na `prales-db-test` (úloha 1, krok 8).
- Produces: běžící verze na `api-test.prales.fun` a `test.prales.fun`.

- [ ] **Step 1: Kontrola před pushem**

Run: `git branch --show-current`
Expected: `testing`

Run: `npx wrangler d1 execute prales-db-test --remote --json --command 'SELECT COUNT(*) AS n FROM pragma_table_info("stadiums") WHERE name = "vip_box"'`
Expected: `n` = 1 (bez sloupce by padaly explicitní SELECTy v `matches.ts`/`teams.ts`/`sponsors.ts`).

Run: `cd apps/api && npx vitest run`
Expected: celá sada zelená (případné předem existující pády nahlásit, neopravovat).

- [ ] **Step 2: Push a čekání na deploy**

```bash
git push origin testing
```

Run: `sleep 80 && gh run list --branch testing --limit 2 --json name,status,conclusion`
Expected: oba běhy (API i web) `completed` / `success`.

- [ ] **Step 3: Ověřit API**

Run: `curl -s "https://api-test.prales.fun/api/teams/302a0ce7-428a-4da8-b4ac-40f27eb9a7d1/stadium" | python3 -c 'import json,sys; d=json.load(sys.stdin); print(d["facilities"].get("vip_box"), d.get("vipBox"), d.get("capacity"), [u for u in d["upgrades"] if u["facility"]=="vip_box"])'`
Expected: `0`, objekt `vipBox` s nulami, nabídka `vip_box` s `cost` 55000 a efektem začínajícím „Prosklená kabina pro pár hostů, −30 míst pro platící diváky". Pokud endpoint vrátí 401, stejný `fetch` spustit v přihlášené záložce přes MCP `javascript_tool` (viz memory `reference_admin_trigger_prod.md`, jen proti test API).

- [ ] **Step 4: Ověřit nákup v prohlížeči**

MCP prohlížeč: `tabs_context_mcp` (createIfEmpty), `navigate` na `https://test.prales.fun/login`; přihlášení testovacím účtem (viz memory `project_current_state.md`, heslo zadává uživatel). Otevřít `https://test.prales.fun/stadion`, sekce Zázemí: karta „VIP lóže" s ikonou 🥂, popis „Hosté sedí mezi diváky", nabídka L1 s cenou v info řádku (ne v tlačítku). Klik „Koupit", potvrdit. Po obnovení: tři tečky úrovně s jednou plnou, řádek „Teď: o 30 míst méně pro platící diváky, provoz 1 500 Kč za domácí zápas…", kapacita stadionu o 30 nižší. Screenshot. Chybový případ: u týmu bez tribuny (nebo po `UPDATE stadiums SET stands = 0` na testovací DB a zpět) karta ukáže zámek „Nejdřív postav aspoň základní tribuny".

Ověřit v DB:

Run: `npx wrangler d1 execute prales-db-test --remote --json --command 'SELECT vip_box, stands FROM stadiums WHERE team_id = "302a0ce7-428a-4da8-b4ac-40f27eb9a7d1"'`
Expected: `vip_box` = 1.

Run: `npx wrangler d1 execute prales-db-test --remote --json --command 'SELECT amount, description FROM transactions WHERE team_id = "302a0ce7-428a-4da8-b4ac-40f27eb9a7d1" AND type = "stadium_upgrade" ORDER BY rowid DESC LIMIT 1'`
Expected: `-55000`, `Vylepšení stadionu: VIP lóže`.

- [ ] **Step 5: Ověřit 3D**

Na `https://test.prales.fun/stadion` otevřít 3D náhled (záložka musí být viditelná, jinak rAF stojí, viz memory `reference_3d_scena_testovani_mcp.md`). Pohled přehled: jedna prosklená kabina za hlavní tribunou (L2+ tribuny: východ; L1: sever). Screenshot. Pro L3 nastavit na testovací DB `UPDATE stadiums SET vip_box = 3 WHERE team_id = "302a0ce7-428a-4da8-b4ac-40f27eb9a7d1"`, obnovit, screenshot řady s markýzou, pak vrátit hodnotu na 1.

- [ ] **Step 6: Ověřit dopady po nejbližším domácím zápase**

Po nejbližším ligovém ticku (16:00 herního dne, testing crony zrcadlí produkci), kdy test tým hrál doma:

Run: `npx wrangler d1 execute prales-db-test --remote --json --command 'SELECT amount, description, reference_id FROM transactions WHERE team_id = "302a0ce7-428a-4da8-b4ac-40f27eb9a7d1" AND description LIKE "VIP lóže%" ORDER BY rowid DESC LIMIT 3'`
Expected: jeden řádek `-1500` na odehraný domácí zápas (žádné duplicity pro stejné `reference_id`).

Když byl na zápas pozván a přijal majitel firmy nebo zastupitel: ověřit `sponsor_team_favor` (`SELECT favor FROM sponsor_team_favor WHERE team_id = "…"`) a `village_team_favor` s `official_id` pozvaného zastupitele; hodnoty mají být vyšší o výsledkovou deltu + 1, resp. o 1. V logu workeru (`npx wrangler tail --env testing` nebo Cloudflare dashboard) hledat `VIP lóže: zastupitelů v lóži` a `bonus lóže 1`.

- [ ] **Step 7: STOP**

Nahlásit uživateli výsledky (API, prohlížeč se screenshoty, DB po zápase). Na `main` nic bez výslovného „nasaď na main". Pro produkci připomenout: nejdřív záloha prod DB (`wrangler d1 export`), pak migrace 0218 na `prales-db-prod`, teprve potom deploy kódu (explicitní SELECTy `vip_box` by bez sloupce padaly).
